# Godot
.godot/
/out/
export_presets.cfg.bak

# Downloaded toolchain (never commit the engine/templates)
godot
*.tpz
*.tmp

# Fetch-classifier scratch — transient build artifacts / tools (gogi-fetch routes here); also .gdignore'd
.gogi-cache/

# Mason toolchain (installed by run.sh; never commit)
/mason/
# verify/preview tooling
verify/
playbooks/
*.mjs
gogi-fetch
web/
