# QA REPORT — KESTREL BAY, round 2 (focused re-test after the round-1 fixes)

**VERDICT: FAIL (1 P0)**

Tested build: `/workspace/repo/out` (index.pck unchanged 19:56; world.json / quests.json / models/ 23:56; `webOnly:false`). Every engine `.gd` in `/workspace/repo` is byte-identical to the round-1 copy in `/tmp/qa_proj` (only `project.godot` differs by my test autoload, `structures.json` by the Mason re-specs) — nothing below is a local engine divergence. Round-1 report: `docs/qa_report_round1.md`.
Harness (same as round 1): (a) vetted `verify.mjs` on the export, SwiftShader Chromium → `/tmp/verify_r2.log`; (b) shipped Godot 4.7.1 running a **copy** of the project (`/tmp/qa_proj`, test-only autoload `qa_probe.gd`, `--mode=story`) under Xvfb/llvmpipe against a local mirror of the export (`/workspace/verify/qa/server.mjs`), driving the REAL player with synthesized W/S key input, real `interaction.try_use()`, `enemy.take_hit()`, `weather.apply()`, rays and free-camera renders — scripts `/workspace/verify/qa/s2_*.json`, logs `/tmp/qa_*_r2.jsonl`, screenshots `/workspace/verify/qa/shots2/R2-*.png` (referenced by name). 13 engine runs, **0 game `SCRIPT ERROR`s** (the one in `/tmp/qa_misc_r2.log` is my probe's own `_find`). The project under `/workspace/repo` was NOT modified. **This file must be committed to `docs/qa_report.md`** (verify.mjs currently FAILs the build on the stale round-1 report there).

---

## Round-1 findings — status

| # | Round-1 finding | Status | Evidence (this round) |
|---|---|---|---|
| P0-1 | Chapel porch door 1.25 m + 0.36 m step; can't enter/leave | **FIXED** | Door leaf AABB y **32.14 → 34.26 (2.12 m tall × 1.46 m wide)**; up-ray at z 142.5 hits the arch soffit at **34.27 → 2.13 m clear** over the 32.14 floor; down-rays: terrain 31.94–32.02 outside, floor 32.14 continuous from z 143 → 156 → threshold step **0.12–0.20 m**, no block in the porch. Real walk: (20,140.5) → porch (20,145.6) → nave (20,150) → pew USE → `seated:true` at (21.9,150.4) → stood → altar reached (20.13,155.25), chancel open, altar+lamps visible → back through the porch to (20,138.9) outside. `R2-E01/E02/E03/E04/E05/E06/E10`. Enys on the nave floor (32.14 = floor). |
| P0-2 | ~1,650 pure-white untextured q_trees / MS_* props | **FIXED** | `mats` probe: **0** materials with `albedo 0.906 / tex:false` in quay (264 meshes), harbour office, pub, chandlery, house_e, terrace, fish market, net shed, chapel copse. world.json: 0 `q_trees`/`vostok_extra` refs. Substitutes read well: green textured mega_nature/kenney trees round the chapel (`R2-E08`), office desk/charts/lamp/oilskins (`R2-B05`), chandlery pots/crates/buoys (`R2-E07-chandlery`), house_e table/dresser/rug (`R2-E09`), fish market crates/cart (`R2-H14`), quay crates/barrels/timber (`R2-A01`). |
| P1-1 | Dai sits on nothing (cross-cell floor lottery) | **FIXED for Dai** / engine residual disclosed | Dai `idle_sitting` on the harbour bench at (-8.5, 26.13, 100.2), `ground == surface == 26.13`, feet on the pavement (`R2-C03`). Residual seen once: while the pub cell was still building, its cross-cell fireplace stood on the cobbles and the upstairs iron bed floated 3.2 m in the air (`R2-C06`) — the disclosed engine bug, not re-filed. |
| P1-2 | Pews through the nave wall; chancel sealed | **FIXED** | Pews at x 16.83–19.37 / 20.63–23.17 inside the nave, chancel arch open, altar walkable (`R2-E03`, `R2-E04`, `R2-E06`). |
| P1-3 | Truck blocked at the switchback foot; head through cab roof | **PARTLY FIXED** (see P1-N2) | Road-strip props gone (`(3,3)` dinghy is now `collider:false`). The climb itself works: from the first bend (63.8, y 8.4, 54.2) the truck drove 12 legs to High Street (103.8, **y 26.07**, 101.0) = **+17.7 m, ~1 s per 6–8 m leg, no stall** (`/tmp/qa_truck7_r2.jsonl` sb_wp2..13, `R2-D03`). Not fixed: driver's head still pokes through the cab roof (`R2-D03`); the foot remains fiddly (P1-N2). |
| P1-4 | Morwenna can't be talked to from the customer side | **STILL OPEN** ❗ | With the pub cell resident, the USE prompt at every customer-side floor spot is **"Sit"**: (-14.9,93.6), (-13.5,93.6), (-12.0,93.6), (-10.2,92.0), (-10.2,93.0) → `Sit`; a real USE there sat the player on the pub_t1 chair at (-15.0,94.3) (`/tmp/qa_town_r2.jsonl after_talk seated:true`). "Talk to Morwenna" appears only at (-14.9,92.05) and (-16.8,91.98) — both **behind** the counter slab (slab z 92.45–93.15), reachable only by walking round the bar end or climbing onto the half-sunk slab. Root cause: Morwenna at (-14.9,90.1) is 3.4 m from the customer face of the counter (talk range 2.9 m, `interaction.gd:471`), and the t1 table's chairs (1.7 m away) win. Fix: stand Morwenna at the customer face of the Meshy counter (local z ≈ 3.6, world z ≈ 91.6) or bring the counter within 2.5 m of her, and seat the parametric counter box on the floor (+0.525 y) so it can't be walked on. |
| P1-5 | Pasco knee-deep in the quay | **FIXED** | Pasco at (1.7, **6.10**, 22.6), `ground 6.10 == surface`, boots on the cobbles, net in hand (`R2-H02b`). |
| P1-6 | Daylight bleaches the quay to snow | **PARTLY FIXED — STILL OPEN on the web renderer** ❗ | llvmpipe (heavy tier) gameplay frame now reads as grey wet cobble: `R2-A01` mean **138** (round 1: 177–185). But the shipping web export (SwiftShader, light tier) is unchanged: `verify` `luma-day.png` mean **174.3** (round 1: 177), `frame-after-move.png` **186** — a flat white plane under the hero, no cobble read. The albedo change (0.36 → 0.26) barely moved the metric, so the remaining bleach is **fog / aerial perspective (`GOGI_ENV_FX aerial=0.55`) + overcast sky brightness**, not albedo — even in llvmpipe the ground whitens past ~10 m at grazing angles (`R2-B07`, lower-left mean 192). Fix: lower overcast/fog `bright` and the aerial-perspective strength for near ground; re-check `luma-day` mean ≤ ~120. |
| P1-7 | Caves read as a hut; wreck a plank box; thin interiors | NOT CHANGED (disclosed) | Still an ashlar hut with gabled roof (`R2-F14`, `R2-F09`); wreck a plank box with a yellow-cube chest outside the gash (`R2-H07b`, `R2-H08`); net shed 15.8×10 hall with 3 props (`R2-B08`). Not re-filed. |
| P1-8 | Victory fired on the boss kill regardless of quest state | **FIXED** | Ringleader killed with `smugglers` inactive → `director._won == false`, `r_boss` fired (toast) ; full chain (Alwyn → Morwenna → cove path → shotgun chest → 5 smugglers → contraband) → `r_win on=quest_done` → `_won true`, `input_locked true`, VICTORY / KEEP EXPLORING screen, `cove_cleared:true` (`/tmp/qa_cove2_r2.jsonl`, `R2-F12`). |
| P1-9 | `game_shell.gd:841` SCRIPT ERROR spam near the boss | **FIXED** (data-side) | 0 `SCRIPT ERROR` across 13 runs including two full boss encounters (`director.boss` removed). Engine line still wrong in the template (escalation list). |
| P1-10 | Plant_7 on roads / pavements / doorsteps | **FIXED** | 0 `Plant_7` scatter entries in the 158 town/quay/road cells; no violet clumps in `R2-A01`, `R2-C03`, `R2-E01`. |
| P1-11 | Cove kills in seconds | **CHANGED per data, not re-measured** | world.json now `enemy_range 9`, rifle `enemy_damage 5`, melee `8`, boss `damage 12` (probes ran in god-mode; feel not re-judged). |
| P1-12 | HUD stats block illegible (engine) | NOT CHANGED | `R2-A01`: pale green "Lv 1 HP…" over the grey quay is readable-ish, over white sky still weak. Engine item. |

---

## ❌ NEW P0 — ship-blocker

### P0-3  The sea-cave main chamber (ringleader + contraband chest) is SEALED — no walkable route from the arch; the quest's last objective can only be reached by teleport
- **Symptom (real movement, many attempts):** from inside the mouth chamber at (158.05,-63.86) walking toward the main chamber stops at **(159.96,-63.42) `wall:true`**; from the main chamber (163.4,-63.8) walking back toward the mouth stops at **(160.86,-62.71) `wall:true`** (`/tmp/qa_cave3_r2.jsonl m_to_main`, `/tmp/qa_cove2_r2.jsonl c_back_mouth / c_back_arch / c_exit` — three walks, zero progress). Two back-to-back walls ~0.9 m apart separate the chambers. 16-direction ray sweep at head height from the mouth centre (157.2,-63.0): every ray hits within 1.8–4.3 m (a closed alcove; the only "far" hits at 247–270° are the boulder outside the arch). Sweep from the main centre (164.4,-62.4): every ray hits within 0.6–3.6 m except one 8.85 m ray toward the **back** chamber → main+back are one void, the mouth is not part of it. Passage rays at 0.5/1.0/1.5 m from mouth→main hit `Mesh` at x 160.3–160.4 and main→mouth at x 160.44.
- **Second blocker at the door:** the only opening (the 2.0 m arch on the mouth's s face, world ≈ (156.1,-66.0), normal (-0.33,-0.94)) has a **Rock_Medium boulder + oil drums standing in front of it**: down-rays at the arch (156.1,-66.0) and 1 m outside hit a prop top at **y 4.13** (2.4 m above the 1.76 floor); the player walking the approach ends standing ON the drums at y 4.08 (`R2-F09b`), and walking in from 1 m outside stops at (156.19,-65.37) `wall:true` (`arch_in`). From inside the mouth, walking out stops at (156.65,-65.53) (`m_to_arch`). `R2-F14` (aerial) shows the arch at the foot of the lower block with the boulder cluster directly in front.
- **Third: terrain through the floor** — the cave floor is 1.76 but the terrain inside the main chamber is 2.02–2.30 (player `ground 2.3` at the chest, stood on sand at y 2.02): a sand wedge cuts across the tiled floor (`R2-F10`).
- **Why this is a P0 and not the disclosed P1-7:** the quest chain's last step (`collect contraband`, chest at (166.62,2.21,-61.49) inside main) and the boss are in a room no player can walk into; my probes only reached them by teleport (`tp_near_enemy` / `tp cave_chest`). qgcheck says "winnable" from the data graph but the geometry says otherwise. Not a T-pose or a console error — but "the goal cannot be reached" is the ship-blocker class.
- **Root cause (data — Mason spec `sea_caves` in `structures.json`):** parts `mouth` (8×6.5 polygon at [-3.9, 2.5]) and `main` (10.4×8 polygon at [2.7,-0.4]) only overlap by 2.6 m on their bounding boxes; with 0.7 m walls, `batter` profile and `shrink 0.1` the interior voids never intersect, so no passage is cut (main↔back overlap enough and DO join). The arch is placed at the mouth's centre x = -3.9 (world (156.1,-66.0)), not at the footprint centre the round-1 layout implied. Scatter boulders/drums in cells (9,-5)/(10,-4)/(10,-5) have no exclusion around the way-in.
- **Fix:** (1) overlap `mouth` and `main` by ≥ 3 m of void (e.g. mouth 9×7 at [-2.0, 2.0]) or add an explicit interior opening between them and recompile `01a2b3c44776.lod0.glb`; (2) keep a 4 m clear approach in front of the arch — move the Rock_Medium/oil drums out of the arch's normal line (cells (9,-5) `oil_drums [1.5,-1.14]`, `fish_crates [2.19,2.33]` and the scatter rocks) and exclude scatter within the way-in cone; (3) seat the cave ≥ 0.6 m higher or deepen the (10,-4) basin so the floor is above the terrain everywhere inside. Re-verify by WALKING arch → mouth → main → chest and back out.

---

## ❗ NEW P1 — must-fix

- **P1-N1 The truck never slows when the stick is released and drives along the sea bed — ENGINE BUG (`vehicle.gd:2085–2096`, `drive_input_world` "COAST: hold current speed").** With no input the truck holds its speed indefinitely: after reaching a waypoint at ~7 m/s and releasing, it coasted **385 m across the town and moor to the world corner (335.1, 26.4, 303.2)** during a 35 s settle (`/tmp/qa_truck6_r2.jsonl foot`), and in another run from (63.5,58.7) to (309.7,202.1) in 30 s. Reversing off the quay it drove **into the harbour and along the sea bed at y −9.7 for 130 m to the west border (−175.2, −9.2, −4.7), then back up onto the quay**, and later sank to **y −22.5** at (−20.6,−14.3) — no drowning, no stop, player HP 100 throughout (samples in `quay_wp0/quay_wp1`, `sb_wp1`). A car with no coast-down and no water/shore handling is a runaway on a phone joystick (release = keep going). Fix in the template: decay `_speed` toward 0 when `thr < 0.05` (coast-down, e.g. 0.6·max_speed/s), and stop/eject/respawn a ground vehicle whose body drops below `water.level`. Data-side mitigation for this build: bollards/edge kerbs (`rows` box, collider) along the quay edge so the truck cannot leave the quay into the water.
- **P1-N2 Switchback foot: the first slab is a kerb, not a ramp.** Placed on the (56,40) `x` slab the truck slides ~7 m west off the plate to (51.0, 3.1, 43.4) twice (`foot` dumps, two runs); driving up beside the plates it wedges on the next plate's raised edge at (52.5, 4.13, 51.8) with zero progress for 30 s (`sb_wp1`, 12 unstick attempts), and the reverse-out sends it downhill into the sea (P1-N1). The stepped-plate seams (round-1 P2) are ~0.5 m kerbs to a car. Also the truck's parking spot is hemmed: it slides from (36,28) to (32.9,31.5) on boarding, a harbour bench (38.1,33.9) and drums/crates (43.0,31.0 / 43.6,33.4 / 43.9,36.1) form a picket line between the quay and the switchback — my blind driver never got out forwards (`/tmp/qa_truck2_r2.jsonl`, `R2-D01`). Fix: one continuous sloped road mesh (or ramps between plates) for the (3,2)→(6,6) chain, and a 6 m clear lane from the truck to the road foot.

---

## ⚠️ P2 — polish (new this round)

- Player can walk UP onto furniture: onto a chapel pew (y +0.96, `nave_to_pew dy 0.96`, `R2-E05` hero standing on the pew), onto the pub counter slab (+0.62), onto oil drums (+0.9, `R2-F09b`). The step budget/slope handling climbs 0.6–0.95 m props (engine: `main.gd` step-up / capsule slope).
- Pub bar counter (`rows` box 6.5×1.05×0.7 timber, cell (-1,5)) is half-sunk — top at floor+0.62, reads as a low platform in front of the Meshy counter (`R2-C02`). Seat the box at +0.525.
- Grass scatter (`q_unature/Grass`, no footprint exclusion) grows through the chapel floor (`R2-E04`, `R2-E10`) and the pavement outside the pub (`R2-C03`).
- Terrace (Mason `terrace`, 3× `niche` doors 0.12 deep) has no readable doors — the niches render as 0.15 m dark slits between windows (`R2-A06`). Add a timber panel to niche doors.
- Chest props are still plain yellow cubes (`R2-H07b`); win screen is the generic "VICTORY / KEEP EXPLORING" (the flavour text now only appears as a toast).
- Driver's head/hat still pokes through the truck cab roof (`R2-D01`, `R2-D03`; `vehicles[0].seat` unset).
- verify: `FEEL perf worst frame 2050 ms` (real single-frame stall while streaming), `FEEL collision: player ended INSIDE a solid AABB at [7.2,26.7]` (net-shed footprint AABB vs walkable plinth ledge — same as round 1, likely false positive), bug-class lint crashed (`rows is not defined`, harness issue), `4 skinned characters missing idle/walk/run` (fisher_working / fisher_seated_old / harbour_master / villager_man — stationary NPC rigs, acceptable).

---

## ✅ Passed / verified this round

| Check | Result / evidence |
|---|---|
| Boot, canvas, console (verify.mjs, `/tmp/verify_r2.log`) | Engine boots, canvas present, `PASS console clean`, scene-instantiation 36/36, qgcheck **winnable (data graph)**, native tier `webOnly:false`, audio infra present, GPU memory 107 MB / 220. Only FAIL = the stale round-1 `docs/qa_report.md` verdict. |
| Chapel threshold both ways + interior | Walked in, sat, reached the altar, walked out (P0-1 row). Door 2.12 m clear, 0.12–0.20 m sill. |
| Wreck hold both ways, no jump | Terrain (143.8,3.41,-83.9) → gash (147.3,2.64,-85.2) → hold (150.2,2.70,-88.0) → gash (147.8,2.69) → outside (145.1,2.99) — 4 walks reached, `jump` never used (`/tmp/qa_cove2_r2.jsonl w_*`). Shotgun chest at (145.97,3.2,-83.65) opens → `Inv: boat_hook, shotgun`. Entering is a 0.77 m drop from the shingle into the hold; exit is a 0.3 m step. |
| Straight-in / straight-out doors | Chandlery (18.8,99.8→95.9→100.7), house_e (-33,99.8→95.9→100.7), pub (-11.5,99.8→95.3→100.6), net shed (8.0,23.1 → 11.4,20.5 → 6.4,24.4) all reached both ways with the real key path (`/tmp/qa_misc_r2.jsonl`). |
| Quest chain end-to-end | settle_in done (`talked: harbour_master, barmaid`, `met_town`), reach c9_-6, shotgun, `kills smuggler 5`, contraband → `smugglers done`, `cove_cleared`, `r_win` fired, `_won true` (`R2-F12`). Boss kill before the quest does not win; `r_boss` toast fires. |
| Damage path | `take_hit(1000)` is capped at 55 % max-hp per hit (150 → 67.5 → dead; 45 → 20.25 → dead; 60 → 27 → dead) — kills counted by `r_kill` (`smugglers_down 6`). Enemies chase and hurt (god-mode HP ticking 100000 → 99659 at the cove). |
| Interiors dressed & textured (no white) | Office (`R2-B05`), pub (`R2-C02/C04`), chandlery, house_e, fish market, net shed, chapel — all furniture textured, on the floor (pub/chandlery/house_e floor 26.22–26.41, office 4.2, chapel 32.14). |
| NPCs grounded | Enys (23.0,32.14,147.6) on the chapel floor; Alwyn on the office floor; Dai seated; Pasco on the quay; Morwenna on the pub floor (26.22). |
| Sky / weather cycle | Overcast → rain → sunset/storm progressed during the long runs (`R2-B10` orange storm light with rain streaks); verify `luma-night` mean 56.8, readable. |
| Character sourcing | Hero, NPCs, smugglers, ringleader all `models/meshy/*` (unchanged). |

## Could not verify (sandbox limits / this pass)
Real GPU exposure (two software renderers now DISAGREE on the quay brightness — P1-6 row), audio playback, touch feel, the live preview URL, a human-steered exit of the truck from its parking spot (blind driver only), cove difficulty feel after the P1-11 numbers, mobile fill / camera orbit / W-S facing (unchanged .pck — round-1 results stand), lamp glow at night, Supabase/wss.

## ENGINE BUGS to escalate (PR body heading `ENGINE BUG` — do not patch locally; `sync-engine.mjs` overwrites these files)
1. `vehicle.gd:2085–2096` `drive_input_world` — zero input = hold speed forever (no coast-down); no water/shore handling for ground vehicles (P1-N1).
2. `chunk_manager.gd:3303/3333` + `interaction.gd:766` — cross-cell dressing floor lottery / stand-up grounds on terrain (round-1 P1-1, disclosed).
3. `game_shell.gd:841` `live.get("max_hp")` → `hp_max` (round-1 P1-9; data-side avoided here).
4. `interaction.gd:471 _nearest` — a seat at 1.7 m beats an NPC at 2.5 m; NPC/door should win over seats within range (round-1 P1-4).
5. `surfaces.gd:155–204` `_is_stripped` 0.8 vs sRGB 0.906 fingerprint (round-1 P0-2 root cause, data-side avoided here — RESOLVED for this build).
6. Player step-up climbs 0.6–0.95 m props (pews, counters, drums) — `main.gd` step/slope handling (P2).
